#include "monty.h"
/**
 * stack_fns - prints the top
 * @head: stack head
 * @counter: line_number
 * Return: no return
*/
void stack_fns(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}
